# 环境配置示例（复制此文件为 .env 并修改）

# 服务器端口
PORT=3001

# 数据库配置（MySQL 8.0）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=safety_system

# JWT 密钥
JWT_SECRET=safety-system-secret-2026

# Redis 配置（可选）
REDIS_HOST=localhost
REDIS_PORT=6379

# 环境
NODE_ENV=development
